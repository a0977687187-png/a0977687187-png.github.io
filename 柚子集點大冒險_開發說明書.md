# 「柚子集點大冒險」兒童行為獎勵系統 — 開發說明書

> 本文件為完整產品規格書，交付給 Claude Code 進行開發。
> 目標平台：手機優先的 Web App（PWA），可安裝到手機主畫面，離線可用。
>
> **App 名稱**：柚子集點大冒險
> **製作人**：張家祥
> 製作人資訊需顯示於：開場啟動畫面（splash screen 下方小字「製作人 張家祥」）與家長模式「設定 → 關於」頁面（App 名稱、版本號、製作人 張家祥）。

---

## 1. 專案概述

### 1.1 產品定位
一套給家長使用的「兒童行為獎勵集點系統」，適合 4~12 歲（幼稚園～小學）的孩子。孩子透過完成家長設定的好行為任務（改掉壞習慣、培養好習慣與愛好）獲得點數，累積點數後可以：

1. **派英雄攻擊怪獸**：具象化的戰鬥畫面，每次攻擊消耗點數，打倒怪獸可獲得家長自訂的實體獎勵。
2. **購買新英雄**：用點數解鎖新角色，價格由家長決定。
3. **兌換獎品**：直接用點數兌換家長設定的獎品。

另外提供**每日小測驗**：依年齡出國語、英語、數學、安全宣導、禮貌宣導題目，隨機 5 題，答對可獲得點數。

### 1.2 教養理論依據（實作時必須落實的設計原則）
本系統依據「改良式集點制度 / 代幣制度（token economy）」的專業建議設計，開發時以下原則必須內建在產品行為中，不是可有可無的裝飾：

| 原則 | 產品落實方式 |
|---|---|
| **獎賞連結**：給點時要明確說出是哪個好行為 | 家長給點時必須選擇對應任務，孩子端畫面會大字顯示「因為你【自己整理書包】獲得 2 點！」並播放慶祝動畫 |
| **獎賞分級**：難改的壞習慣給高點數、容易的給低點數 | 每個任務的點數由家長自訂（1~20 點），新增任務時 UI 提示「越難做到的行為，建議給越多點」 |
| **社會性獎勵**：點數之外要搭配口頭讚美 | 給點成功畫面會隨機顯示一句鼓勵語（如「爸爸媽媽以你為榮！」），並提示家長「記得給孩子一個擁抱或口頭讚美」 |
| **規則事先訂好、標準一致** | 所有任務、點數、獎勵、扣點規則都在家長模式中明文設定，孩子端全部看得到，避免臨時加碼或隨心情增減 |
| **目標行為具體可行** | 新增任務時提供範例與提示：「請寫具體行為，例如『不跟阿嬤頂嘴』而不是『當個乖寶寶』」 |
| **立即回饋** | 給點操作 3 秒內完成（家長模式首頁就有快速給點按鈕），孩子端立即看到動畫回饋 |
| **點數門檻不可過高** | 家長設定怪獸血量／獎品價格時，系統依「孩子每日平均可得點數」試算預估達成天數，超過 21 天會出現黃色警告 |
| **間歇性增強與退場機制** | 任務可設定「連續增強」或「間歇增強」模式（見 3.2.4）；系統每月提醒家長檢視是否可將已養成的習慣調降點數或移除 |
| **可扣點但規則要事先講清楚** | 扣點必須綁定事先建立的「扣點規則」，不能任意扣，孩子端可查看所有扣點規則 |
| **固定兌換時間** | 家長可設定每週固定兌獎日（例如週日晚上），孩子端首頁顯示兌獎日倒數 |

### 1.3 角色設計方針（重要）
⚠️ **不得使用任何現有版權角色**（例如哆啦A夢、蠟筆小新、胖虎、細菌人、哥吉拉等日本知名卡通角色均受著作權保護，不可作為內建角色使用，也不可製作明顯模仿的近似設計）。

改採兩個方案並行：

1. **內建原創角色**：以「日系可愛卡通風格」設計一套原創英雄與怪獸（見第 5 節角色清單），用 SVG/CSS 繪製，風格圓潤、大眼、Q 版，孩子熟悉且喜愛的美術語彙，但角色本身為原創。
2. **自訂角色**：家長可上傳照片或圖片作為英雄／怪獸的頭像（存 base64 於本機），並自訂名稱。家庭私人使用時，家長可自行決定上傳什麼圖片。

---

## 2. 使用者角色與模式

### 2.1 雙模式設計
| 模式 | 進入方式 | 功能 |
|---|---|---|
| **孩子模式**（預設） | 開啟 App 即為孩子模式 | 看點數、打怪獸、買英雄、做測驗、看任務清單與獎品、看紀錄 |
| **家長模式** | 點右上角齒輪 → 輸入 4 位數 PIN 碼 | 所有設定與管理：給點/扣點、任務管理、怪獸管理、英雄商店定價、獎品管理、題庫設定、孩子管理 |

- 首次啟動時進入「初始設定精靈」：設定家長 PIN → 新增孩子（姓名、年齡/年級、頭像）→ 從範本挑選任務 → 挑選第一隻怪獸 → 完成。
- 支援**多個孩子**，孩子模式左上角可切換孩子（各自獨立的點數、英雄、進度）。
- PIN 錯誤 5 次鎖定 1 分鐘。PIN 忘記時可用「安全問題」重設（初始設定時填寫）。

---

## 3. 功能模組規格

### 3.1 孩子模式首頁
- 頂部：孩子頭像＋姓名、目前點數（大字、金幣圖示，點數變動時數字滾動動畫）。
- 中央：目前挑戰中的怪獸卡片（怪獸圖、名稱、血量條、「去戰鬥！」按鈕）。
- 下方四個大按鈕（圖示＋文字，適合兒童操作，最小點擊區域 64px）：
  1. ⚔️ 打怪獸
  2. 📝 每日測驗
  3. 🦸 英雄商店
  4. 🎁 獎品兌換
- 底部資訊列：今日已獲得點數、距離兌獎日倒數、我的任務清單入口。
- 全程使用注音友善的簡短文字（小學低年級可讀），重要按鈕以圖示為主。

### 3.2 任務與集點模組（家長管理）

#### 3.2.1 任務資料欄位
```
Task {
  id: string
  childId: string          // 屬於哪個孩子
  title: string            // 具體行為，如「自己整理書包」
  type: "改掉壞習慣" | "培養好習慣" | "學習愛好" | "家事協助"
  points: number           // 1~20
  reinforceMode: "連續" | "間歇"   // 見 3.2.4
  dailyLimit: number       // 每日最多可得次數（預設 1）
  active: boolean
}
```

#### 3.2.2 內建任務範本（依年齡分組，新增任務時可一鍵套用）
- **幼稚園（4-6 歲）**：自己穿鞋襪、自己收玩具、不挑食吃完飯、上床時間到不吵鬧、跟長輩打招呼、刷牙不用催
- **小學低年級（7-9 歲）**：放學自己完成回家功課、閱讀一本故事書、練琴/才藝練習 30 分鐘、睡前整理書包、不跟兄弟姊妹吵架、幫忙倒垃圾
- **小學中高年級（10-12 歲）**：主動預習/複習、運動 30 分鐘、協助非例行家事（如洗冷氣濾網）、控制 3C 使用時間、寫日記
- 每個範本附建議點數，家長可修改。

#### 3.2.3 給點流程（家長模式首頁的核心操作，必須 3 步內完成）
1. 選孩子 → 2. 點任務卡片 → 3. 確認送出。
- 送出後孩子端（若同裝置則切回孩子模式時）播放「獲得點數」全螢幕慶祝動畫：金幣飛入點數欄＋顯示行為名稱＋隨機鼓勵語。
- 提供「額外表現加點」：家長可針對某任務一次給 1.5~3 倍點數並輸入原因（例如「今天特別認真」），對應「讚賞努力過程」的原則。

#### 3.2.4 間歇性增強機制
- 任務設為「間歇」模式後，系統依設定機率（預設 60%）決定該次是否給點；未給點時孩子端仍顯示「太棒了！你做到了！」的社會性讚美動畫（無點數），維持行為動機並降低物質依賴。
- 家長模式的任務列表顯示每個任務近 30 天達成率，達成率 > 85% 連續兩週時，系統提示：「這個習慣快養成了！建議改為間歇給點或調降點數。」

#### 3.2.5 扣點
- 家長需先建立「扣點規則」（如：對長輩大聲頂嘴 −2 點），孩子端「規則」頁可查看全部扣點規則。
- 執行扣點時必選規則，不可自由輸入負點數。點數最低扣到 0，不出現負數。

### 3.3 英雄打怪獸模組（核心玩法）

#### 3.3.1 玩法流程
1. 家長建立怪獸（見 3.3.3），設定血量、每次攻擊所需點數、擊倒獎勵。
2. 孩子在「打怪獸」頁看到目前怪獸，點「攻擊！」按鈕 → 扣除攻擊所需點數 → 播放戰鬥動畫 → 怪獸血量減少。
3. 血量歸零 → 播放怪獸倒下＋勝利動畫 → 顯示「恭喜擊倒【搗蛋菌菌怪】！獲得獎勵：去動物園玩一天！」→ 產生一張「獎勵兌換券」進入孩子的獎勵券匣，家長模式可將券標記為「已兌現」。
4. 擊倒後家長可啟用下一隻怪獄（可預先排隊多隻怪獸形成「關卡」）。

#### 3.3.2 戰鬥畫面規格（必須具象化，這是本產品的靈魂）
- 橫幅場景：左側站英雄（目前出戰的英雄）、右側站怪獸，背景依怪獸屬性變換（森林／城市／火山等，CSS 漸層＋簡單 SVG 景物即可）。
- 怪獸上方顯示血量條（紅色，帶數字 例：35 / 50）與怪獸名稱。
- 英雄下方顯示「攻擊一次需要 ⭐5 點」與孩子目前點數。
- **攻擊動畫序列**（用 CSS animation / Web Animations API 實作，總長約 2.5 秒）：
  1. 英雄向前衝刺（translateX）＋ 揮擊動作（rotate 擺動）
  2. 攻擊命中：怪獸閃白／震動（shake）、彈出傷害數字（每次攻擊固定造成 1 點傷害的倍數可由家長設定，預設 1 攻擊 = 1 傷害）
  3. 命中特效：星星／爆裂粒子（SVG particles）
  4. 英雄退回原位，怪獸血條以動畫減少
- 每個英雄有不同的攻擊特效顏色與招式名稱（畫面彈出招式字卡，如「香蕉迴旋斬！」）。
- 怪獸血量低於 30% 時表情變成「快哭了」的可憐表情（增加趣味），倒地動畫：旋轉倒下＋冒星星＋「咚」音效。
- 音效：攻擊、命中、勝利號角（使用 Web Audio 內建合成或極短的內嵌 base64 音效），提供家長模式靜音開關。
- 點數不足時攻擊鈕反灰，顯示「還差 ⭐3 點，去完成任務吧！」並附「看我的任務」捷徑。

#### 3.3.3 怪獸管理（家長模式 CRUD）
```
Monster {
  id: string
  childId: string
  name: string             // 如「懶惰泥怪」
  avatar: string           // 內建怪獸 id 或上傳圖片 base64
  maxHp: number            // 家長自訂，例如 20
  costPerAttack: number    // 每次攻擊消耗點數，家長自訂
  reward: string           // 擊倒獎勵（文字描述），如「麥當勞兒童餐一份」
  status: "排隊中" | "挑戰中" | "已擊倒"
  currentHp: number
  defeatedAt?: string
}
```
- 新增/編輯畫面即時試算：「若孩子每天約得 X 點，打倒這隻怪獸約需 Y 天」，Y > 21 顯示警告「門檻可能太高，孩子容易放棄」。
- 已擊倒的怪獸進入「怪獸圖鑑」，顯示擊倒日期與當時獲得的獎勵，成為孩子的成就收藏。

### 3.4 英雄商店

```
Hero {
  id: string
  name: string
  avatar: string           // 內建英雄 id 或上傳圖片
  price: number            // 購買所需點數，家長可逐一修改定價
  skillIds: string[]       // 此英雄擁有的招式（至少 1 個，可日後新增）
  defaultSkillId: string   // 預設出戰招式
  packId: string           // 來自哪個內容包（"core" 為初版內建）
}

Skill {
  id: string
  name: string             // 招式名稱（戰鬥時彈出的字卡），如「香蕉迴旋斬！」
  effectColor: string      // 特效主色
  animationType: string    // 動畫模板代號：slash / punch / beam / spin / magic / throw
  particleShape: string    // 粒子形狀：star / heart / bubble / bolt / flame
  unlockCost: number       // 0 = 隨英雄附贈；> 0 = 需用點數解鎖（家長可改價）
  packId: string
}
```
- **招式系統**：每位英雄可擁有多個招式，孩子在「出戰設定」中選擇要使用的招式；新招式可用點數解鎖（價格家長可調），解鎖播放「習得新招式！」動畫。
- 戰鬥動畫由「動畫模板（animationType）＋ 特效色 ＋ 粒子形狀」三個參數組合而成，**新增招式只需新增一筆 Skill 資料，不需要寫新程式**——這是日後持續推出新招式的關鍵設計，動畫模板需一開始就實作齊 6 種。
- 每個孩子預設擁有 1 位初始英雄，其餘英雄以「剪影＋價格」呈現，點數足夠即可購買（跳出確認框，防誤觸需長按或二次確認）。
- 購買成功播放「新英雄登場！」開箱動畫。
- 已擁有的英雄可在「出戰設定」中選擇一位出戰。
- 家長模式可：調整每位英雄價格、上下架英雄、新增自訂英雄（上傳圖片＋命名＋招式名）。
- 購買英雄花費的點數與打怪花費相同來自同一個點數池，讓孩子練習「儲蓄 vs. 消費」的取捨（延遲滿足訓練）。

### 3.5 每日小測驗模組

#### 3.5.1 規則
- 孩子每天可作答最多 N 回合（家長可設 1~3，預設 1）。
- 每回合從題庫**隨機抽 5 題**，全部答完後結算：
  - 5 題全對：獲得滿分獎勵點數（家長設定，預設 5 點）＋「全對！」煙火動畫
  - 答對 3-4 題：獲得部分點數（預設每題 1 點）
  - 依家長設定也可改為「全對才給點」模式
- 答錯的題目立即顯示正確答案與一句簡短說明（教育性回饋）。
- 題目型式：單選題（3~4 選項），選項為大按鈕，附注音或圖示輔助（低年齡層）。

#### 3.5.2 題庫結構與分級
```
Question {
  id: string
  ageGroup: "幼稚園" | "小一小二" | "小三小四" | "小五小六"
  subject: "國語" | "英語" | "數學" | "安全宣導" | "禮貌品格"
  question: string
  options: string[]        // 3~4 個
  answerIndex: number
  explanation: string      // 答錯時顯示
}
```
- 家長在孩子資料中設定年齡層，測驗自動抽該年齡層題目；家長可勾選要包含哪些科目。
- **內建題庫**：每個年齡層 × 每科目至少內建 20 題（共 400 題起），由開發時直接寫入 seed 資料。範例：
  - 幼稚園／安全：「過馬路的時候要走哪裡？」（斑馬線／馬路中間／隨便走）
  - 幼稚園／禮貌：「收到禮物要說什麼？」（謝謝／不要／哼）
  - 小一小二／數學：「7 + 5 = ?」（11／12／13）
  - 小一小二／國語：「『大』的相反是什麼？」（小／多／高）
  - 小三小四／英語：「『蘋果』的英文是？」（Apple／Banana／Orange）
  - 小五小六／安全：「網路上有陌生人要你傳照片，應該怎麼做？」（告訴爸媽或老師／偷偷傳給他／跟他聊天）
- 家長模式可**新增自訂題目**、停用不想出的內建題。
- 同一天內已答過的題目不重複抽出（當日去重）。

### 3.6 獎品兌換模組
```
Prize {
  id, childId, name, cost(點數), stock(可選, 預設不限), active
}
```
- 建議家長建立 3 個價位帶（引導 UI：小獎 10~30 點／中獎 50~100 點／大獎 150 點以上），訓練孩子思考與延遲滿足。
- 兌換需家長模式確認（輸入 PIN）後扣點，產生兌換紀錄。
- 若家長設定了「每週兌獎日」，非兌獎日孩子端兌換鈕顯示「週日晚上才能兌換哦，還有 X 天」。（打怪獎勵券不受此限，擊倒即得券。）

### 3.7 紀錄與統計
- **孩子端**：簡單的「我的紀錄」— 最近獲得點數的清單（日期＋行為＋點數）、怪獸圖鑑、獎勵券匣。讓孩子「看見自己的進步」。
- **家長端**：
  - 每個孩子近 7 / 30 天的每日得點折線圖（用 CSS/SVG 簡單繪製即可，不必引入大型圖表庫）
  - 各任務達成次數排行、測驗答對率（分科目）
  - 完整流水帳（給點、扣點、攻擊、購買、兌換），可匯出 JSON 備份／匯入還原

---

## 4. 頁面清單與導覽結構

```
孩子模式
├─ 首頁（點數、當前怪獸、四大功能入口）
├─ 打怪獸（戰鬥畫面）
├─ 每日測驗（答題流程 → 結算頁）
├─ 英雄商店（含出戰設定）
├─ 獎品兌換
├─ 我的任務（唯讀：任務清單、點數規則、扣點規則）
└─ 我的紀錄（點數紀錄、怪獸圖鑑、獎勵券匣）

家長模式（PIN 進入）
├─ 快速給點/扣點（首頁）
├─ 任務管理（含範本庫）
├─ 怪獸管理（含關卡排隊）
├─ 英雄商店管理
├─ 獎品管理
├─ 題庫管理
├─ 孩子管理（新增/編輯孩子、年齡層）
├─ 統計報表
├─ 設定（PIN、兌獎日、音效、備份匯出/匯入、內容包管理）
└─ 關於（App 名稱：柚子集點大冒險、版本號、製作人 張家祥）
```

---

## 5. 內建原創角色清單（美術規格）

全部以 **inline SVG** 繪製（不依賴外部圖片），Q 版日系卡通風格：大頭身比例（約 1:1.5）、粗輪廓線、大眼、明亮配色、圓潤造型。每個角色需要：待機姿勢、攻擊/受擊表情共 2~3 個狀態。

### 英雄（初始送第 1 位，其餘由家長定價販售）
| 名稱 | 造型概念 | 招式名稱 | 特效色 |
|---|---|---|---|
| 小勇者阿光 | 戴平底鍋頭盔、拿木劍的小男孩 | 元氣斬！ | 黃 |
| 布丁忍者 | 布丁造型頭巾的忍者貓 | 焦糖手裡劍！ | 橘 |
| 星星魔法師 | 星星法杖的小女孩 | 流星許願擊！ | 紫 |
| 機器狗汪答 | 圓滾滾的藍色機器狗 | 火箭飛拳！ | 藍 |
| 恐龍寶貝噴噴 | 綠色小暴龍，背著書包 | 泡泡龍息！ | 綠 |
| 貓咪超人喵俠 | 披紅色小披風的橘貓 | 肉球連打！ | 紅 |

### 怪獸（家長建立怪獸時可從中挑選造型，血量/點數/獎勵自訂）
| 名稱 | 造型概念 | 象徵的壞習慣（顯示於家長端說明） |
|---|---|---|
| 懶惰泥怪 | 攤在地上的黏土泥巴怪 | 拖拖拉拉、賴床 |
| 搗蛋菌菌怪 | 紫色病菌造型、壞笑 | 不洗手、不刷牙 |
| 大吼霸王龍 | 體型很大的暴躁恐龍 | 亂發脾氣、大吼大叫 |
| 挑食妖精 | 捏著鼻子的綠色小妖精 | 挑食 |
| 3C 章魚 | 八隻腳各拿一台手機的章魚 | 3C 成癮 |
| 亂丟大王 | 全身掛滿玩具雜物的邋遢怪 | 不收玩具、房間亂 |
| 頂嘴鸚鵡王 | 戴皇冠、張大嘴的鸚鵡 | 頂嘴、不禮貌 |
| 夜貓蝙蝠 | 睜著大眼不睡覺的蝙蝠 | 不肯睡覺 |

> 命名與造型皆為原創；「象徵壞習慣」的設計讓孩子打倒的怪獸與要改掉的行為產生心理連結（打倒 3C 章魚 = 戰勝 3C 成癮）。

---

## 6. 技術規格

### 6.1 技術選型
- **單一 HTML/JS Web App（PWA）**：React（或 Vue）＋ Vite 建置，行動優先 RWD（設計基準 375px 寬）。
- 不需後端伺服器：所有資料存 **localStorage / IndexedDB**（IndexedDB 優先，因需存 base64 圖片）。提供 JSON 匯出/匯入做備份與換機轉移。
- PWA：manifest ＋ service worker，可加入主畫面、離線使用。
- 動畫：CSS animation / Web Animations API；粒子特效用輕量 SVG/Canvas 自寫，不引入大型遊戲引擎。
- 音效：短音效以 base64 內嵌或 Web Audio 合成。
- 不使用瀏覽器以外的權限；上傳圖片以 `<input type="file" accept="image/*">` 讀入並壓縮（長邊 ≤ 512px）後轉 base64 儲存。

### 6.2 資料模型總覽（IndexedDB stores）
`children / tasks / monsters / heroes / skills / heroOwnership / skillUnlocks / prizes / questions / pointLedger（流水帳）/ rewardCoupons / contentPacks / settings`

- `settings` 內需存 `schemaVersion`（資料結構版本號），App 啟動時若偵測到舊版資料，依序執行 migration 函式升級，**確保未來改版不會弄丟舊資料**。

- 所有點數變動一律寫入 `pointLedger`：`{ id, childId, delta, reason, refType(task/attack/quiz/purchase/redeem/adjust), refId, timestamp }`，孩子目前點數 = ledger 加總（另存快取值以加速）。

### 6.3 品質要求
- 兒童 UI：按鈕大、字級大（孩子端正文 ≥ 18px）、色彩對比充足、操作皆有動畫與音效回饋。
- 防呆：所有扣點/消費行為需確認；家長操作全部在 PIN 保護內。
- 效能：戰鬥動畫在中階手機需順暢（避免大量 DOM 重排，動畫用 transform/opacity）。
- 語言：繁體中文（台灣用語）。
- 程式碼結構清晰、元件化，資料層與 UI 分離，內建 seed 資料（角色、任務範本、400+ 題庫）獨立成檔。

---

## 7. 驗收條件（Definition of Done）


1. 首次啟動可完成初始設定精靈並建立第一個孩子與任務。
2. 家長給點後，孩子端點數即時更新並播放含行為名稱的慶祝動畫。
3. 戰鬥頁可完整呈現：英雄衝刺 → 命中特效 → 傷害數字 → 血條動畫 → 血量歸零時勝利動畫與獎勵券發放。
4. 點數不足時無法攻擊/購買，並有引導提示。
5. 家長可完整 CRUD：任務、怪獸（含血量/攻擊點數/獎勵）、英雄定價、獎品、自訂題目。
6. 測驗每回合隨機 5 題、依年齡層與勾選科目抽題、當日不重複、答錯顯示解析、結算正確給點。
7. 英雄購買後出現在「出戰設定」，切換出戰英雄會改變戰鬥畫面角色與招式字卡。
8. 扣點僅能透過預先建立的扣點規則執行，點數不會為負。
9. 資料重新整理/關閉 App 後不遺失；JSON 匯出後可在另一台裝置匯入還原。
10. 所有內建角色與圖像皆為原創，未包含任何現有版權角色。
11. 招式系統可運作：英雄可切換招式、用點數解鎖新招式、不同招式呈現不同動畫組合；新增一筆 Skill 資料即可在遊戲中出現新招式（無需改程式）。
12. 內容包匯入可運作：匯入一個測試用內容包 JSON（含 1 位新英雄＋2 個新招式＋1 隻新怪獸造型＋10 題新題目）後，內容正確出現在商店/怪獸選擇/題庫中，且移除內容包不影響既有玩家資料。
13. 啟動畫面與「關於」頁正確顯示 App 名稱「柚子集點大冒險」與「製作人 張家祥」。
14. 資料結構含 schemaVersion，並示範至少一個 migration 範例（可為空升級），確保未來版本升級路徑存在。

---

## 8. 長期營運與擴充架構（重要：本 App 會長期使用並持續更新）

本產品預計使用多年並持續推出新英雄、新招式、新怪獸與新題目，因此**內容必須與程式碼徹底分離**，架構上遵守以下規範：

### 8.1 資料驅動（Data-driven）內容系統
- 所有英雄、招式、怪獸造型、題庫都定義在**內容資料檔**（`/src/content/` 下的 JSON ＋ SVG 元件註冊表），程式只負責讀取與呈現，**新增內容 = 新增資料，不改遊戲邏輯**。
- SVG 角色以「元件註冊表」管理：`avatarRegistry[avatarId] → SVG 元件`，新角色即新增一個 SVG 元件並註冊 id。
- 招式動畫採「模板組合式」設計（見 3.4 的 Skill 模型）：6 種動畫模板 × 特效色 × 粒子形狀，可組合出數十種不重複的招式表現，之後再擴充新模板也只是加一種模板。

### 8.2 內容包（Content Pack）機制
```
ContentPack {
  packId: string           // 如 "pack-2027-summer"
  name: string             // 如「2027 夏日更新包」
  version: string
  heroes: Hero[]
  skills: Skill[]
  monsterDesigns: MonsterDesign[]   // 怪獸造型（名稱＋SVG/圖片＋象徵壞習慣說明）
  questions: Question[]
}
```
- App 內建 `core` 內容包（初版全部內容）。
- 之後發布新版本時，把新內容做成新的內容包一併打包進 App；App 啟動時比對 `contentPacks` store，**以 packId＋version 增量合併**：只新增缺少的內容，絕不覆蓋或刪除玩家已擁有的英雄、進度與家長設定（家長改過的價格以家長設定為準）。
- 家長模式「設定 → 內容包管理」可：檢視已安裝內容包、**手動匯入內容包 JSON 檔**（提供格式驗證與錯誤提示）、停用某內容包（隱藏其商店內容，但孩子已購買的不受影響）。
- 開發時需附一份 `content-pack-template.json` 範本與格式說明文件（`CONTENT_PACK.md`），讓日後（包含請 Claude Code 再次協助時）能快速產生新內容包。

### 8.3 版本升級與資料安全
- `schemaVersion` ＋ migration 管線：每次資料結構變動都新增一個 migration 函式，啟動時自動逐版升級，舊用戶更新 App 後資料完整保留。
- 匯出的備份 JSON 需包含 schemaVersion 與已安裝內容包清單，匯入時自動跑相同的 migration，保證跨版本可還原。
- App 版本號顯示於「關於」頁（柚子集點大冒險 v1.0.0，製作人 張家祥），並在程式中集中管理（單一常數檔）。

### 8.4 預留的未來擴充點（本版先不做，但架構不可阻擋）
- 英雄升級/進化（同一英雄的第二型態 = 新 avatar ＋ 新招式的資料組合）
- 節慶限定怪獸與限時關卡（Monster 已有排隊機制，加上起訖日期欄位即可）
- 多裝置同步（資料層已集中於單一 repository 模組，之後可替換為雲端後端）

---

## 9. 開發順序建議（給 Claude Code 的里程碑）

1. **M1 資料層與家長模式**：IndexedDB 封裝、schemaVersion/migration 管線、初始設定精靈、孩子/任務 CRUD、給點/扣點與流水帳。
2. **M2 孩子模式基礎**：首頁、點數顯示、任務清單、獲得點數動畫。
3. **M3 戰鬥系統**：怪獸 CRUD、戰鬥畫面、6 種招式動畫模板、獎勵券、怪獸圖鑑。
4. **M4 英雄與招式**：內建英雄 SVG 註冊表、購買流程、招式解鎖與切換、自訂英雄。
5. **M5 測驗系統**：題庫 seed、抽題邏輯、答題 UI、結算給點、題庫管理。
6. **M6 內容包系統**：core 內容包重構、匯入/停用 UI、格式驗證、`content-pack-template.json` 與 `CONTENT_PACK.md` 文件。
7. **M7 收尾**：獎品兌換、統計報表、備份匯出入、PWA 化、啟動畫面與關於頁（製作人 張家祥）、音效與細節打磨。
